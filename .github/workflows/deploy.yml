name: Deploy validator service

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Run tests
        run: python -m unittest discover -s tests

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Build Lambda artifact
        run: zip -j validator.zip index.py validation.py

      - name: Upload artifact to S3
        run: aws s3 cp validator.zip s3://${{ vars.ARTIFACT_BUCKET }}/validator.zip

      - name: Update Lambda code
        run: |
          aws lambda update-function-code \
            --function-name validator-service \
            --s3-bucket ${{ vars.ARTIFACT_BUCKET }} \
            --s3-key validator.zip
